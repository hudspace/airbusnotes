



/*function newPage() {

	//Select an option from the dropdown menu and put it in the 'ntsb' variable
	const ntsb = document.getElementById('mobile_menu');

	//get the value of the selected option, in this case a url (now in the ntsb variable), and assign it to the 'report' variable
	const report = ntsb.value;

	//Load the page associated with the newly assigned url value
	window.location.assign(report);
}